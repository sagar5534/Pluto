import { AccountRepository } from 'src/repositories/account.repository';
import { TransactionRepository } from './transaction.repository';

export const repositories = [AccountRepository, TransactionRepository];
